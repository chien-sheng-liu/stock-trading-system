## Copy this file to `.env` and fill in real values.
## The backend reads these via python-dotenv; `.env` is git-ignored.

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=stock
DB_USER=postgres
DB_PASS=change_me

# OpenAI (optional; enables AI summaries)
# Use an instruct-capable model for the Completions API path.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-3.5-turbo-instruct

FINMIND_TOKEN=
